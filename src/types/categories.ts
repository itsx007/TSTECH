export type Category = {
    id: string;
    slug: string; // "thermal-safety-calorimetry"
    name: string; // "Thermal Safety & Calorimetry"
    localizedName: string; // "热安全与量热"
    description: string;
    heroImage?: string;
    seriesSlugs: string[]; // ["arc", ...]
  };
  